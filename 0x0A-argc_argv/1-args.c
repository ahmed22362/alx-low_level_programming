#include <stdio.h>

/**
* main - Entry point print number of argument passed to
*
*@argc: the count of arguments
*@argv: vector of string contain the passed arguments
*
* Return: 0 - success
*/

int main(int argc, char *argv[] __attribute__((unused)))
{
	printf("%d\n", argc);

	return (0);
}
