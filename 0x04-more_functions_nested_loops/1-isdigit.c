#include "main.h"

/**
* _isdigit - function to check the given input is digit or not
* 
*@c: the input number
*
*Return : 1 if n is digit otherwise
*/

int _isdigit(int c)
{
	if ((c >= 48) && (c < = 57))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
